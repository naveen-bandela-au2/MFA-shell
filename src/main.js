const Home = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      <h1>this is the Main page from first app</h1>
    </div>
  );
};

export default Home;
